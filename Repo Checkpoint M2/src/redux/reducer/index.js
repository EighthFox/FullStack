import {GET_CHARACTERS, GET_CHARACTER_DETAIL, CREATE_CHARACTER, DELETE_CHARACTER, GET_SHIPS} from "./../../redux/actions/index";


const initialState = {
  characters: [],
  characterDetail: {},
  ships: [],
};

const rootReducer = (state = initialState, action) => {
  switch (action.type) {
 
  }
};

export default rootReducer;
